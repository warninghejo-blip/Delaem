# Финальные инструкции: Поиск en и U.$i

## ❓ Что осталось найти:

### 1. `en` - класс хеша (MD5 или SHA-256?)

**Где используется:**
```javascript
function ei(S) {
    return new en().update(S).digest((0, U.ms)())
}
```

**Что искать в файле `app-2637cbcbd7da64c9.js`:**

**Вариант 1: Поиск по имени**
- `const en =`
- `let en =`
- `var en =`
- `import en from`
- `en = MD5`
- `en = SHA256`

**Вариант 2: Поиск по содержимому**
- `crypto-js` - найдет импорт библиотеки
- `MD5` - найдет если используется MD5
- `SHA256` или `SHA-256` - найдет если используется SHA-256
- `require('crypto-js` - найдет require
- `from 'crypto-js` - найдет import

**Вариант 3: Поиск по использованию**
- `new en()` - найдет где создается экземпляр
- `.update(S).digest` - найдет функцию ei

### 2. `U.$i()` - что возвращает?

**Где используется:**
```javascript
J += "\n" + I + e_ + X((0, U.$i)());
```

Где `X` по умолчанию = `eu`, а `eu = S => N.a7[7] + N.Em[0] + N.Em[1]`

**Что искать в файле `app-2637cbcbd7da64c9.js`:**

**Вариант 1: Поиск по имени**
- `U.$i`
- `U["$i"]`
- `U['$i']`
- `function $i`
- `const $i =`

**Вариант 2: Поиск по использованию**
- `U.$i()` - найдет где вызывается
- `(0, U.$i)()` - найдет вызов

**Вариант 3: Поиск по содержимому**
- `$i` - найдет все упоминания
- `return ""` - может возвращать пустую строку
- `return "unisat"` - может возвращать константу

## 📋 Пошаговая инструкция:

### Шаг 1: Откройте файл
1. DevTools → Sources
2. Найдите файл `app-2637cbcbd7da64c9.js`
3. Откройте его

### Шаг 2: Поиск `en`
1. Нажмите Ctrl+F
2. Введите: `const en =`
3. Нажмите Enter
4. Если не найдено, попробуйте:
   - `crypto-js`
   - `MD5`
   - `SHA256`
   - `new en()`

### Шаг 3: Поиск `U.$i`
1. Нажмите Ctrl+F
2. Введите: `U.$i`
3. Нажмите Enter
4. Если не найдено, попробуйте:
   - `U["$i"]`
   - `$i`
   - `(0, U.$i)()`

### Шаг 4: Скопируйте найденное
Когда найдете определения, скопируйте их и отправьте мне.

## 🎯 Альтернатива: Тестирование

Если не можете найти эти функции, можно:
1. Протестировать текущую реализацию (SHA-256 без дополнительной строки)
2. Если не работает, попробовать MD5
3. Если не работает, попробовать добавить пустую строку после `@#?.#@`

## 💡 Подсказка:

Если поиск не дает результатов, возможно:
- `en` импортируется из другого модуля (ищите в начале файла)
- `U.$i` может быть определен в другом месте (ищите в других файлах)
- Код может быть обфусцирован (имена функций изменены)

