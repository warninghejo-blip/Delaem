# Тестирование текущей реализации x-sign

## ✅ Текущая реализация:

1. **Кодирование параметров** (`ep`) - ✅ Реализовано
2. **Удаление домена** (`ed`) - ✅ Реализовано
3. **Удаление префиксов** (`ef`) - ✅ Реализовано
4. **Разделитель** (`e_ = "@#?.#@"`) - ✅ Реализовано
5. **Хеширование** - ⚠️ Используется SHA-256 (первые 32 символа)
6. **Дополнительная строка после @#?.#@** - ⚠️ Не добавляется (может быть нужна)

## 🧪 Как протестировать:

### 1. Запустите аудит на сайте

1. Откройте сайт `fennecbtc.xyz`
2. Подключите кошелек
3. Нажмите "GET YOUR ID"
4. Дождитесь завершения аудита

### 2. Проверьте в консоли браузера:

Откройте DevTools → Console и проверьте:
- Есть ли ошибки 403 Forbidden от Uniscan/InSwap?
- Есть ли ошибки 429 Too Many Requests?
- Загружаются ли данные (networth, age, counts)?

### 3. Проверьте в Network:

1. Откройте DevTools → Network
2. Найдите запросы к `fennec-api.warninghejo.workers.dev`
3. Проверьте запросы с `action=fractal_audit`
4. Посмотрите ответ - есть ли ошибки?

### 4. Проверьте debug информацию:

В ответе API должен быть объект `debugInfo` с информацией:
- `x_sign_auto_generated: true` - x-sign генерируется автоматически
- `uniscan_x_sign: "..."` - сгенерированный x-sign для Uniscan
- `inswap_x_sign: "..."` - сгенерированный x-sign для InSwap

## 🔍 Что проверить:

### Если работает (нет ошибок 403):
✅ Текущая реализация правильная!
- SHA-256 работает
- Без дополнительной строки после `@#?.#@` работает

### Если ошибки 403 Forbidden:
❌ Нужно исправить:
1. **Возможно используется MD5 вместо SHA-256:**
   - Нужно добавить MD5 библиотеку для Cloudflare Workers
   - Или найти способ использовать MD5

2. **Возможно нужна дополнительная строка после @#?.#@:**
   - Попробовать добавить пустую строку: `J += '\n' + timestamp + '@#?.#@' + '';`
   - Или попробовать другие варианты

### Если ошибки 429 Too Many Requests:
⚠️ Это не проблема x-sign, а проблема rate limiting
- Увеличить задержки между запросами
- Улучшить кэширование
- Использовать правильные headers

## 📋 План действий:

1. **Протестировать текущую реализацию**
2. **Если 403 ошибки:**
   - Попробовать MD5 вместо SHA-256
   - Попробовать добавить пустую строку после `@#?.#@`
3. **Если работает:**
   - Отлично! Текущая реализация правильная

## 🛠️ Если нужно изменить реализацию:

### Вариант 1: Попробовать MD5
Нужно будет добавить MD5 библиотеку для Cloudflare Workers

### Вариант 2: Добавить пустую строку
Изменить строку 152 в `worker.js`:
```javascript
J += '\n' + timestamp + '@#?.#@' + '';
```

### Вариант 3: Попробовать константу
Изменить строку 152 в `worker.js`:
```javascript
J += '\n' + timestamp + '@#?.#@' + 'unisat';
```

## 📝 Отчет о тестировании:

После тестирования сообщите:
1. ✅ Работает или ❌ Не работает
2. Какие ошибки (если есть)
3. Что показывается в debugInfo

