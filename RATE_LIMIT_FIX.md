# Исправления Rate Limiting (429 ошибки)

## Внесенные изменения согласно рекомендациям UniSat API

### 1. Экспоненциальный Backoff
- ✅ Начальная задержка: **2 секунды** (было 3)
- ✅ Увеличение: **2, 4, 8, 16, 30 секунд** (максимум 30s, рекомендация UniSat)
- ✅ Исправлено во всех местах: `all_balance`, `my_pool_list`, `genesis_tx`

### 2. Кэширование
- ✅ Увеличено с 10 до **30 секунд** (рекомендация UniSat: 5-30 секунд для UI)
- ✅ Кэш применяется для:
  - Runes balance-list
  - BRC-20 summary
  - Ordinals inscription-data
  - History queries

### 3. Throttling
- ✅ Установлено **2000ms** между запросами к UniSat API (0.5 req/s)
- ✅ Рекомендация UniSat: 1-5 req/s max
- ✅ Все запросы к UniSat API проходят через throttling

### 4. Последовательное выполнение
- ✅ Запросы выполняются **последовательно**, не параллельно
- ✅ `all_balance` и `my_pool_list` выполняются один за другим
- ✅ Это снижает нагрузку на API и предотвращает 429 ошибки

### 5. Retry логика
- ✅ При получении 429:
  1. Проверяется заголовок `Retry-After` (если есть, используется)
  2. Применяется экспоненциальный backoff: 2 → 4 → 8 → 16 → 30 секунд
  3. Максимум 5 попыток
  4. Никогда не повторяем сразу (всегда ждем задержку)

## Рекомендации UniSat API

Согласно документации: https://unisat.io/open-api

### ✅ Реализовано:
- [x] Request throttling (2000ms между запросами)
- [x] Exponential backoff (2, 4, 8, 16, 30 секунд)
- [x] Response caching (30 секунд)
- [x] Sequential execution (не параллельно)
- [x] Retry-After header support

### 📝 Дополнительные рекомендации:
- Используйте API key если доступен (улучшает лимиты)
- Избегайте polling - используйте event-based логику
- Кэшируйте неизменяемые данные (метаданные, списки) на минуты

## Результат

После этих изменений:
- ✅ Меньше 429 ошибок благодаря правильному throttling
- ✅ Быстрее ответы благодаря кэшированию
- ✅ Правильная обработка rate limits через exponential backoff
- ✅ Соблюдение рекомендаций UniSat API

