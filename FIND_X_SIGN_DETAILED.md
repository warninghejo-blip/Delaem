# Детальная инструкция: что искать в \_app-2637cbcbd7da64c9.js

## ✅ Что мы УЖЕ знаем:

1. **Код интерцептора** (вы уже предоставили):

```javascript
S.interceptors.request.use(S => {
    let I = Math.floor(Date.now() / 1e3); // timestamp
    let J = (S.baseURL || '') + S.url;
    J = J.replace(ed, ''); // убираем домен

    // GET параметры
    if (S.method === 'get') {
        // ... добавляем параметры через ep(T) ...
        J += '\n';
        J = J.replaceAll('•', '%E2%80%A2');
    }

    // POST данные
    if (S.method === 'post') {
        J += '\n';
        J += JSON.stringify(S.data);
    }

    // КРИТИЧНО: Вот эта строка!
    J += '\n' + I + e_ + X((0, U.$i)());

    // Установка headers
    S.headers[ea((0, U.tt)())] = ei(J); // x-sign = ei(J)
    S.headers[eo((0, U.Q9)())] = I; // x-ts = I

    Z(S); // Вызывается ПОСЛЕ установки x-sign
    return S;
});
```

2. **Функция Z(S)** (найдена ранее):

```javascript
function Z(S) {
    let O = S.headers['X-Sign']; // Использует уже готовый x-sign
    S.headers['cf-token'] = '...'; // Генерирует cf-token из x-sign
}
```

## 🔍 Что нужно найти (3 критических момента):

### 1. **`X((0, U.$i)())` - что возвращает?**

**Где искать:**

- В файле `_app-2637cbcbd7da64c9.js` найдите:
    - `U.$i` или `U["$i"]` или `U['$i']`
    - `function X(` или `const X =` или `X =`
    - `X((0,` - вызов функции X

**Что это может быть:**

- Пустая строка `""`
- Константа `"unisat"` или `"uniscan"` или `"fractal"`
- Еще один timestamp
- Случайная строка или UUID

**Пример того, что искать:**

```javascript
// Вариант 1: Константа
U.$i = function () {
    return 'unisat';
};
X = function (s) {
    return s;
};

// Вариант 2: Пустая строка
U.$i = function () {
    return '';
};
X = function (s) {
    return s;
};

// Вариант 3: Что-то другое
U.$i = function () {
    return Date.now().toString();
};
```

**Как найти:**

1. Откройте `_app-2637cbcbd7da64c9.js` в DevTools Sources
2. Поиск: `U.$i` (Ctrl+F)
3. Найдите определение `U.$i` или `U["$i"]`
4. Найдите функцию `X` (поиск: `function X` или `const X`)

---

### 2. **`ei(J)` - функция хеширования (MD5 или SHA-256)?**

**Где искать:**

- В файле `_app-2637cbcbd7da64c9.js` найдите:
    - `function ei(` или `const ei =` или `ei =`
    - `new en()` - это класс хеша
    - `.digest(` - метод получения хеша

**Что искать:**

```javascript
// Пример того, что может быть:
function ei(S) {
    return new en().update(S).digest((0, U.ms)());
}

// Нужно найти, что такое `en`:
// Вариант 1: MD5
const en = require('crypto-js/md5');
// или
import MD5 from 'crypto-js/md5';
const en = MD5;

// Вариант 2: SHA-256
const en = require('crypto-js/sha256');
// или
import SHA256 from 'crypto-js/sha256';
const en = SHA256;
```

**Как найти:**

1. Поиск: `function ei` или `const ei =`
2. Найдите строку `new en()`
3. Найдите определение `en` (поиск: `const en =` или `import en` или `en =`)
4. Проверьте, это MD5 или SHA-256

---

### 3. **`ep(T)` - функция кодирования параметров (уже реализована, но проверить)**

**Где искать:**

- В файле `_app-2637cbcbd7da64c9.js` найдите:
    - `function ep(` или `const ep =` или `ep =`

**Что искать:**

```javascript
// Текущая реализация в worker.js:
const encodedValue = encodeURIComponent(params[key])
    .replace(/'/g, '%27')
    .replace(/%3A/gi, ':')
    .replace(/%24/g, '$')
    .replace(/%2C/gi, ',')
    .replace(/%20/g, '+')
    .replace(/%5B/gi, '[')
    .replace(/%5D/gi, ']');
```

**Проверьте:**

- Все ли правила кодирования учтены?
- Может быть дополнительные правила?

---

## 🎯 Быстрый способ найти все сразу:

1. **Откройте DevTools → Sources → `_app-2637cbcbd7da64c9.js`**

2. **Поставьте breakpoint в интерцепторе:**
    - Найдите строку: `J += "\n" + I + e_ + X((0, U.$i)());`
    - Поставьте breakpoint перед этой строкой

3. **Сделайте запрос на странице Uniscan:**
    - Откройте любую страницу с адресом
    - Дождитесь остановки на breakpoint

4. **В консоли DevTools выполните:**

```javascript
// Проверьте значение X((0, U.$i)())
console.log('U.$i:', U.$i());
console.log('X result:', X((0, U.$i)()));

// Проверьте тип хеша
console.log('en:', en);
console.log('ei test:', ei('test'));

// Проверьте ep
console.log('ep test:', ep('test'));
```

5. **Изучите переменные:**
    - В Scope или Variables найдите `U`, `X`, `ei`, `en`, `ep`
    - Скопируйте их определения

---

## 📋 Что прислать:

Когда найдете, пришлите:

1. **Определение `U.$i`:**

```javascript
U.$i = function() { ... }
// или
U["$i"] = function() { ... }
```

2. **Определение `X`:**

```javascript
function X(...) { ... }
// или
const X = function(...) { ... }
```

3. **Определение `ei` и `en`:**

```javascript
function ei(S) { ... }
const en = ...;  // MD5 или SHA-256?
```

4. **Определение `ep` (если отличается от текущего):**

```javascript
function ep(T) { ... }
```

---

## 🔧 Альтернативный способ (если не можете найти в коде):

1. **Откройте DevTools → Network**
2. **Найдите запрос к Uniscan с `x-sign`**
3. **Скопируйте:**
    - URL запроса
    - `x-sign` значение
    - `x-ts` значение

4. **В консоли выполните:**

```javascript
// Попробуйте разные варианты X((0, U.$i)())
const testVariants = [
    '', // пустая строка
    'unisat', // константа
    'uniscan', // константа
    'fractal', // константа
    Date.now().toString() // timestamp
];

// Для каждого варианта проверьте хеш
testVariants.forEach(variant => {
    const J = '/fractal-api/explorer-v1/address/summary?address=...\n1765934147@#?.#@' + variant;
    // Вычислите хеш и сравните с x-sign
});
```

---

## ⚠️ КРИТИЧНО:

**`Z(S)` НЕ генерирует `x-sign`!** Она вызывается ПОСЛЕ того, как `x-sign` уже установлен, и использует его для генерации `cf-token`.

**Основные неизвестные:**

1. `X((0, U.$i)())` - что добавляется после `@#?.#@`
2. `ei(J)` - MD5 или SHA-256?
3. `ep(T)` - все ли правила кодирования учтены?
