# Инструкция по анализу файла _app-2637cbcbd7da64c9.js

Файл минифицирован и очень большой, поэтому автоматический поиск затруднен.

## Что нужно найти:

1. **Функция генерации x-sign:**
   - Ищите функцию, которая создает строку `J`
   - Ищите функцию, которая делает хеш от строки `J`
   - Ищите установку `headers["x-sign"]` или `headers["X-Sign"]`

2. **Ключевые слова для поиска:**
   - `x-sign` или `X-Sign`
   - `x-ts` или `x-ts`
   - `@#?.#@` (разделитель)
   - `ei(` (функция хеширования)
   - `new en()` (создание хеш-объекта)
   - `.update(` (обновление хеша)
   - `.digest(` (получение результата)

3. **Функция Z (генерация cf-token):**
   - `function Z(S)`
   - `S.headers["X-Sign"]`
   - `cf-token`
   - `substring(12, 14)`

## Как найти:

1. Откройте файл в редакторе (VS Code, Notepad++, и т.д.)
2. Используйте поиск (Ctrl+F) по ключевым словам выше
3. Когда найдете код, скопируйте его и отправьте мне

## Что прислать:

Когда найдете код генерации x-sign, скопируйте:
1. Всю функцию генерации (от `function` до `}`)
2. Контекст вокруг (несколько строк до и после)
3. Название функции и где она вызывается

