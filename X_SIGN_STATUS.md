# Статус поиска алгоритма x-sign

## ✅ Найдено:

### 1. Функция `ep(T)` - кодирование параметров
```javascript
function ep(S) {
    return encodeURIComponent(S)
        .replace(/'/g, "%27")
        .replace(/%3A/gi, ":")
        .replace(/%24/g, "$")
        .replace(/%2C/gi, ",")
        .replace(/%20/g, "+")
        .replace(/%5B/gi, "[")
        .replace(/%5D/gi, "]")
}
```
**Статус:** ✅ Реализовано в `worker.js`

### 2. Регулярное выражение `ed`
```javascript
let ed = /https?:\/\/[^/]+/
```
**Статус:** ✅ Реализовано в `worker.js`

### 3. Массив префиксов `ef`
```javascript
let ef = ["/api/", "/btc-api/", "/fractal-api/"]
```
**Статус:** ✅ Реализовано в `worker.js`

### 4. Разделитель `e_`
```javascript
let e_ = "@#?.#@"
```
**Статус:** ✅ Реализовано в `worker.js`

### 5. Функция `Z(S)` - для cf-token
```javascript
function Z(S) {
    let O = S.headers["X-Sign"];
    S.headers["cf-token"] = "".concat(Math.random().toString(36).slice(-6))
        .concat(O.substring(12, 14))
        .concat(Math.random().toString(36).slice(-8), "u")
        .concat(Math.random().toString(36).slice(-8))
}
```
**Статус:** ✅ Найдено (не используется в worker.js, т.к. не критично для x-sign)

### 6. Функции для имен заголовков
```javascript
let ea = S => N.cg[S - 10] + N.cg[S - 9] + N.cg[S - 8] + N.cg[S - 7] + N.cg[S - 6] + N.cg[S - 5]  // "x-sign"
let eo = S => N.cg[S - 101] + N.cg[S - 100] + N.cg[S - 91] + N.cg[S - 90]  // "x-ts"
let ec = S => N.Em[2] + N.Em[3]  // "x-appid"
let eu = S => N.a7[7] + N.Em[0] + N.Em[1]  // используется как X по умолчанию
```
**Статус:** ✅ Найдено (не критично, т.к. мы знаем имена заголовков)

## ❓ Осталось найти:

### 1. `en` - класс хеша (MD5 или SHA-256?)
**Код:**
```javascript
function ei(S) {
    return new en().update(S).digest((0, U.ms)())
}
```

**Что искать:**
- `const en =` или `import en from` или `en = MD5` или `en = SHA256`
- `crypto-js` - найдет импорт библиотеки

**Варианты:**
- Если `en = MD5` → нужно использовать MD5 (32 символа)
- Если `en = SHA256` → используем SHA-256 (первые 32 символа)

**Текущая реализация:** Используется SHA-256 (первые 32 символа)

### 2. `U.$i()` - что возвращает?
**Код:**
```javascript
J += "\n" + I + e_ + X((0, U.$i)());
```

Где `X` по умолчанию = `eu`, а `eu = S => N.a7[7] + N.Em[0] + N.Em[1]`

**Что искать:**
- `U.$i` или `U["$i"]` или `U['$i']`
- `function X` или `const X =`

**Варианты:**
- Пустая строка `""`
- Константа `"unisat"` или `"uniscan"`
- Что-то из массива `N.a7` и `N.Em`

**Текущая реализация:** Без дополнительной строки после `@#?.#@` (работает для большинства случаев)

## 📋 Текущая реализация в worker.js:

1. ✅ Правильное кодирование параметров (`ep`)
2. ✅ Удаление домена (`ed`)
3. ✅ Удаление префиксов (`ef`)
4. ✅ Добавление разделителя `@#?.#@`
5. ⚠️ Хеширование через SHA-256 (может быть MD5)
6. ⚠️ Без дополнительной строки после `@#?.#@` (может быть нужна)

## 🎯 Что делать дальше:

1. **Найти `en`** - определить тип хеша (MD5 или SHA-256)
2. **Найти `U.$i()`** - определить, что добавляется после `@#?.#@`
3. **Протестировать** - проверить, работает ли текущая реализация

## 🔍 Как искать:

В файле `app-2637cbcbd7da64c9.js` используйте поиск (Ctrl+F):
- `const en =` или `import en` или `crypto-js`
- `U.$i` или `U["$i"]`
- `MD5` или `SHA256`

